# LiteLLM proxy aliases (versions_dep/v2_c2/litellm/config.yaml). Committed, no secrets.
version: 1
roles:
  fast_text:
    aliases:
      - gpt-hub-fast
      - gpt-hub-strong
      - gpt-hub-fallback
  reasoning_code_local:
    aliases:
      - gpt-hub-turbo
      - gpt-hub-reasoning-or
      - gpt-hub-strong
      - gpt-hub-fallback
  reasoning_code_openrouter:
    aliases:
      - gpt-hub-reasoning-or
      - gpt-hub-strong
      - gpt-hub-doc
      - gpt-hub-fallback
  vision_general:
    aliases:
      - gpt-hub-vision
      - gpt-hub-fallback
  doc_synthesis:
    aliases:
      - gpt-hub-doc
      - gpt-hub-strong
      - gpt-hub-fallback
