# Disabled during active v4 control-loop development.
# Prod catalog comes from orchestrator live refresh, not weekly packaged YAML commits.
# Re-enable manually via workflow_dispatch when baseline drift checks are needed again.

name: OpenRouter free catalog check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Weekly catalog PR disabled — see versions_dep/v4 README Validation"
